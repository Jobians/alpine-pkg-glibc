name: Build Alpine Package

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Fix git safe directory
      run: git config --global --add safe.directory /github/workspace

    - name: Build the new release version
      uses: jobians/action-abuild@master
      with:
        PACKAGE_PATH: "." # path to your APKBUILD
        # Optionally pass keys if you want to sign packages
        rsa_private_key: ${{ secrets.RSA_PRIVATE_KEY }}
        rsa_public_key: ${{ secrets.RSA_PUBLIC_KEY }}

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: alpine-apk-package
        path: |
          /home/runner/packages/**/*.apk
          /home/runner/packages/**/*.asc
